version: '3.1'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: integration_tests_db
      POSTGRES_USER: sa
      POSTGRES_PASSWORD: sa
    ports:
      - "5432:5432"
